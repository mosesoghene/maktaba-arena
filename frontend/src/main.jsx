import React from 'react'
import ReactDOM from 'react-dom/client'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <h1>Hello, Maktaba Arena with Hot-Reload! (3)</h1>
  </React.StrictMode>,
)
